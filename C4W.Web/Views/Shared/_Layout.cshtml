<!DOCTYPE html>
<html>
<head>
	<link rel="icon" href="@Url.Content("~/favicon.ico")" type="image/x-icon"/>
	<link rel="shortcut icon" href="@Url.Content("~/favicon.ico")" type="image/x-icon"/>
	<meta charset="utf-8"/>
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>@ViewBag.Title</title>
	@Styles.Render("~/Content/css")
	@Scripts.Render("~/bundles/modernizr")
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
</head>
<body>
<!-- child of the body tag -->
	<span id="top-link-block" class="hidden">
		<a href="#top" class="btn btn-primary" onclick="$('html,body').animate({ scrollTop: 0 }, 'slow');return false;">
		<i class="glyphicon glyphicon-chevron-up"></i>
		</a>
	</span><!-- /top-link-block -->
<nav class="navbar navbar-fixed-top navbar-color">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-2">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<img src="~/Content/Images/logo-c4w.png" class="img-responsive"/>
		</div>
		<div class="collapse navbar-collapse">
			<ul class="nav navbar-nav" style="margin-top: 70px;">
				<li><a class="link-nav-color" href="~/Home/About">SOBRE NOSOTROS</a></li>
				<li><a class="link-nav-color" href="#">CERTIFICACIONES</a></li>
				<li><a class="link-nav-color" href="#">SERVICIOS</a></li>
				<li><a class="link-nav-color" href="#">CALENDARIO</a></li>
				<li><a class="link-nav-color" href="#">CONTÁCTANOS</a></li>
			</ul>
		</div>
	</div>
</nav>
<div>
	@Html.Partial("_LoginPartial")
	@RenderBody()
	<br/>
	<br/>
	<footer>
		<div class="row" style="margin-top: 20px;">
		    <div class="col-lg-12 col-xs-12 col-md-12 col-sm-12" style="padding: 0px 0px 0px 0px;">
		        <img src="~/Content/Images/imagenesNuevas/footer.PNG" class="img-responsive" style="width: 100%;"/>
		    </div>
		</div>
	</footer>
</div>

@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/jqueryval")
@Scripts.Render("~/bundles/bootstrap")
@RenderSection("scripts", required: false)
<script>
	// Only enable if the document has a long scroll bar
	// Note the window height + offset
	if (($(window).height() + 100) < $(document).height()) {
		$('#top-link-block').removeClass('hidden').affix({
			// how far to scroll down before link "slides" into view
			offset: { top: 100 }
		});
	}
</script>

<script>
	// Toggle menu
	$(document).ready(function($) {
		$('.toggle-menu').jPushMenu();
	});

	$('#lev-0 .wbutton').on('click',
		function(e) {
			$('#lev-0 .wbutton').removeClass('op0');
			$(this).addClass('op0');

			$('#lev-11 > div').fadeOut('fast');
			$('#lev-2 > div').fadeOut('fast');

			$('#lev-1 > div').each(function() {
				$(this).fadeOut(0);
			});

			var element = $(this).data('element');
			$(element).fadeIn('fast');

			var target = $(this).data('target');
			doScroll(target);
		});

	$('#lev-1 .wbutton-xl, #lev-11 .wbutton-xl').on('click',
		function(e) {
			e.preventDefault();

			$('#lev-2 > div').fadeOut(0);
			var element = $(this).data('element');
			$(element).fadeIn('fast');

			var target = $(this).data('target');
			if (target == '#lev-11') {
				$('#lev-1 > div').each(function() {
					$(this).fadeOut(0);
				});
			}
			doScroll(target);
		});

	function doScroll(target) {
		var $target = $(target);
		$('html, body').stop().animate({
				'scrollTop': $target.offset().top
			},
			900,
			'swing',
			function() {
				window.location.hash = target;
			});
	}

	// Smooth scrolling
	$(document).ready(function() {
		$('.smooth').on('click',
			function(e) {
				e.preventDefault();

				var target = $(this).data('target');
				doScroll(target);
			});


		$('a[data-ajax-menu]').on('click',
			function() {
				var objective = $(this).data('ajax-menu');
				if ($(objective).is(':visible')) {
					$(objective).fadeOut('fast');
				} else {
					$(objective).fadeIn('fast');
				}
			});
	});
</script>
</body>
</html>